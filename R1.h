#pragma once
#include <string>

class R1 {
public:
	int stR1 = 0; // number of requests in the queue in one moment
	std::string stringR1 = ""; // outputable changes in queue R1 in a single moment
	
	
};